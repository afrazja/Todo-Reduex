var React = require('react');
var PropTypes = require('prop-types');

var Todo = React.createClass({

  render: function() {
    return (
      <div >
      test
      </div>
    );
  }

});

module.exports = Todo;
